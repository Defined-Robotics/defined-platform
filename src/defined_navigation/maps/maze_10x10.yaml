# maze_10x10.yaml
# Map metadata for nav2 map_server.
#
# Maze layout:
#   Wall A: y=+2.0, x=-5.0 to +1.0 (east gap: x=+1 to +5)
#   Wall B: y=-2.0, x=-1.0 to +5.0 (west gap: x=-5 to -1)
#
# Origin: bottom-left corner of the image in the map frame.
# 200x200 image at 0.05 m/px, room centred at (0,0) → bottom-left at (-5,-5).

image: maze_10x10.pgm
resolution: 0.05
origin: [-5.0, -5.0, 0.0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.25
